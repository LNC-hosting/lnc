const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
  theme: {
    fontFamily: {
      sans: "var(--font-space-grotesk)",
      pixel: "var(--font-pixel)",
    },
  },
};

export default config;
